Drop `.schem` files here (Sponge or MCEdit schematic format).

Reference them by name (with or without the `.schem` extension) in the
"Meow, build `<name>` at `<x>` `<y>` `<z>`." chat command or the control
panel's Build field.
